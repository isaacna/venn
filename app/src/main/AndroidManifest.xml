<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.isaacna.projects">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
 
    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_round"
        android:label="Venn"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".CommunitiesActivity" />
        <activity android:name=".MatchesActivity" />
        <activity android:name=".CandidateActivity" />
        <!-- <activity -->
        <!-- android:name=".Create" -->
        <!-- android:label="@string/title_activity_create" -->
        <!-- android:theme="@style/AppTheme.NoActionBar" /> -->
        <activity android:name=".CreateActivity" />
        <activity android:name=".FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".oneMatchActivity"
            android:label="@string/title_activity_one_match"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".JoinCommActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".AddCommunityActivity" />
        <!-- <activity -->
        <!-- android:name=".addCommunity" -->
        <!-- android:label="@string/title_activity_add_community" -->
        <!-- android:theme="@style/AppTheme.NoActionBar" /> -->
        <!-- <activity android:name=".createCommunity" /> -->
        <activity android:name=".CreateCommunityActivity" />
        <activity android:name=".JustCreatedActivity" />
        <activity
            android:name=".MessagesActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".CreateAccountActivityOriginal" />
        <activity android:name=".EditCommParamsActivity" />
        <activity android:name=".SwipesForCommunityActivity" />
        <activity android:name=".MatchProfileActivity" />
        <activity android:name=".changePhotoActivity" />
        <activity android:name=".EditActivity" />
        <activity android:name=".EditBioActivity"></activity>
    </application>

</manifest>